package org.hishmalif.data.enums;

public enum QueryConstants {
    SELECT,
    AS,
    OVER,
    PARTITION_BY,
    FROM,
    JOIN,
    ON,
    WHERE,
    AND,
    OR,
    LOWER,
    GROUP_BY,
    ORDER_BY,
    LIMIT
}